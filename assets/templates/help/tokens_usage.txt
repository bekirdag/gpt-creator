Usage: gpt-creator tokens [--project PATH] [--details] [--json]

Print aggregated adapter token usage captured under .gpt-creator/logs/usage.ndjson (falls back to codex-usage.ndjson for legacy runs). Per-call rows show adapter, task, stage, model, and tokens in/out/total.
