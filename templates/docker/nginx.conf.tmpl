# nginx.conf template for reverse proxy (API, web, admin)
events {}
http {
  server {
    listen 80;
    server_name _;

    location / {
      proxy_pass http://web:5173/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
      proxy_read_timeout 600s;
      proxy_connect_timeout 60s;
      proxy_intercept_errors on;
      error_page 404 502 503 504 = @spa_root;
    }

    location /admin/ {
      proxy_pass http://admin:5173/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
      proxy_read_timeout 600s;
      proxy_connect_timeout 60s;
      proxy_intercept_errors on;
      error_page 404 502 503 504 = @spa_admin;
    }

    location /api/ {
      proxy_pass http://api:3000/api/;
      proxy_set_header Host $host;
    }

    location @spa_root {
      proxy_pass http://web:5173/index.html;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
    }

    location @spa_admin {
      proxy_pass http://admin:5173/index.html;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
    }
  }
}
